export default {
  valheimSchedule: {
    noApiInfo:
      "Valheim has no REST API — players can't be warned about the restart in advance. Shutdown attempts a clean CTRL+C (with a full world save) first; a forced shutdown only happens as a last resort if the server doesn't respond.",
  },
};
