{
  "permissions": {
    "allow": [
      "Bash(yarn create:*)",
      "WebFetch(domain:mui.com)",
      "WebFetch(domain:www.electronjs.org)",
      "Bash(yarn add:*)",
      "WebFetch(domain:docs.palworldgame.com)"
    ]
  },
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{ const f=JSON.parse(d)?.tool_input?.file_path; if(f) process.stdout.write(f); })\" | { read -r f; [ -n \"$f\" ] && yarn --cwd 'e:/repo/server-forge' fix:prettier \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Prettier..."
          }
        ]
      }
    ]
  }
}
